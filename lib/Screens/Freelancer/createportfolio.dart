import 'package:flutter/material.dart';

class CreatePortfolio extends StatelessWidget {
  const CreatePortfolio({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Padding(
        padding: EdgeInsets.symmetric(horizontal: 20),
        child: Column(),
      ),
    );
  }
}
